apiVersion: v1
kind: Service
metadata:
  name: doctor-service
  namespace: default
  labels:
    app: doctor-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3003
      protocol: TCP
      name: http
  selector:
    app: doctor-service
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
