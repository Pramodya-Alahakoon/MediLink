apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secrets
  namespace: default
  labels:
    app: rabbitmq
type: Opaque
stringData:
  # Replace with secure credentials for production
  rabbitmq-user: "guest"
  rabbitmq-password: "guest"
