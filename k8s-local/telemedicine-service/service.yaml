apiVersion: v1
kind: Service
metadata:
  name: telemedicine-service
  namespace: medilink
  labels:
    app: telemedicine-service
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
  selector:
    app: telemedicine-service
