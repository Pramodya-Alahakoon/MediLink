apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: medilink
  labels:
    app: api-gateway
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30001
      protocol: TCP
  selector:
    app: api-gateway
