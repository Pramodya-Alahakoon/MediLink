apiVersion: v1
kind: Service
metadata:
  name: doctor-service
  namespace: medilink
  labels:
    app: doctor-service
spec:
  type: ClusterIP
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
  selector:
    app: doctor-service
