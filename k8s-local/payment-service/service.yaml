apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: medilink
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
    - port: 3005
      targetPort: 3005
      protocol: TCP
  selector:
    app: payment-service
