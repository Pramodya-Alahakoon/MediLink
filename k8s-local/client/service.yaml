apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: medilink
  labels:
    app: client
spec:
  type: NodePort
  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30000
      protocol: TCP
  selector:
    app: client
