apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: medilink
  labels:
    app: patient-service
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app: patient-service
