apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: medilink
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: auth-service
