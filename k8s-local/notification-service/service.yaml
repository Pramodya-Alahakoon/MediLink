apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: medilink
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
    - port: 5003
      targetPort: 5003
      protocol: TCP
  selector:
    app: notification-service
